#include <stdio.h>
/* Cet algorithme informe si le produit de deux nombres est positif ou negatif*/
int main()
{
  float a ;
  printf("Donnez le nombre");
  scanf("%f",&a);
  if (a<0)
  {
    printf("Le nombre est negatif");
  }
  else
  {
    printf("Le nombre est positif");
  }
  return 0;
}
  
